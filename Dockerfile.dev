FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN apt-get update \
  && apt-get install -y --no-install-recommends git procps iproute2 net-tools lsof \
  && rm -rf /var/lib/apt/lists/*
RUN npm ci

COPY . .

EXPOSE 5173
CMD ["tail", "-f", "/dev/null"]
